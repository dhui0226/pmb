import Cards from './Cards'

const ProjectsCompleted = (user={user}) => {
    return (
        <div>
            <h1>
                Completed
            </h1>
            <Cards />
        </div>
    )
}

export default ProjectsCompleted